/*
  * Count search occurances
  * Write a function that takes an array and a search
  * element as arguments and returns the number of occurances of
  * the search element in the array.
 */
function countSearchOccurances(arrayToSearch, elementsToSearch) {
  // your code goes here
}

// usage:
const versionArray = [1, 2, 5, 7, 2];
const versionCount = (countSearchOccurances(versionArray, 2));
console.log('versionCount: ', versionCount); // 2



const ageArray = [21, 25, 22, 25, 30, 25, 30];
const ageCount = (countSearchOccurances(ageArray, -25));
console.log('ageCount: ', ageCount); // 0
